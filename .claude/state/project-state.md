# Project state index

<!--
Curated project-state index. Loaded at every SessionStart via
`.claude/scripts/sessionstart-inject-state.sh`. Target: 40-60 lines.
Regenerate at session end per `.claude/rules/worklog.md`. Newest decisions at top.
WORKLOG.md is the append-only audit log; grep it for history.
-->

# D-DEE Data Ops — present-moment snapshot

_Last regenerated: 2026-04-30 late evening UTC — Bug #1 (Aariz/Houssam seed swap) + Bug #2 (empty `fct_outreach` cascade across 4 marts) BOTH RESOLVED end-to-end via PRs #141, #142, #143, #144. SDR-attribution chain fully repaired: `Marts.fct_speed_to_lead.setter_dim_display_name` shows correct setter names; `Marts.lead_journey.{assigned_sdr_name,number_of_dials}` flipped from 0% pop to 5.3%/9.4%. Aariz reportable for the first time post-Y1-cutover._

## Where we are

- **`bq-ingest` live revision is `00103-rav`** as of 2026-04-30 14:15 UTC. Five changes since `00083-5kk`: (1) `00084-dfl` retired the dead `/ask`/`/query`/`/query/catalog` HTTP analytical surface (PR #133); (2) `00099-jez` added `users`/`tags` to `GHL_OBJECT_TYPES` env (revealed users API contract failure); (3) `00101-xas` added `GHL_ENDPOINT_TAGS=/locations/yDDvavWJesa03Cv3wKjt/tags` env (fixed tags polling — 45 rows now flowing); (4) `00103-rav` shipped PR #138 code fix for `/users/search` (companyId param + no-page param) + added `GHL_COMPANY_ID` env (16 user rows now flowing); (5) C1 review-driven follow-up (commit `8e8978c` on main; explicit `limit=100` for /users/search) **shipped to main but NOT yet redeployed** — landing on next bq-ingest deploy. Rollback path: `update-traffic --to-revisions=bq-ingest-00084-dfl=100`.
- **`bq-ingest` revision `00103-rav`** includes everything in `00099-jez` + PR #138 (users polling code fix). All 10 GHL entity types now succeed end-to-end on `/ingest-ghl` invocation. `00099-jez`, `00101-xas`, `00084-dfl`, `00093-xiv` retained as rollback options.
- **dbt prod auth runs via Workload Identity Federation** (PR #126). Workflows authenticate to `sa-transform@project-41542e21-470f-4589-96d` via OIDC; pool `github-actions`, provider `github-oidc`. The `GCP_SA_KEY_PROD` secret is **dormant** — no workflow uses it. Org policy `constraints/iam.disableServiceAccountKeyCreation` blocks new SA-key creation; WIF is the only forward path.
- **dbt prod schemas are STG / Core / Marts** (capitalized — Y1 cutover, PR #129). dbt-built tables coexist alongside bq-ingest's inline-SQL writers in those datasets (different table names, no conflict). `dbt_project.yml` `+schema:` config + profiles.yml fallback `dataset: Marts` aligned. Snapshots route to Core (PR #130 fix removed a model-level override that was silently sending them to lowercase `snapshots`).
- **dbt nightly is GREEN.** Latest run `25170736965` (workflow_dispatch, 14:21 UTC, on-demand-triggered to verify users polling fix end-to-end): SUCCESS. dim_users now 16 rows; `fct_calls_booked.assigned_user_sk` non-NULL = 10/5,487 (the rest are LEFT JOIN orphans — bookings whose pre-booking opp is owned by historical/deleted/automation users not returned by `/users/search`; not a bug, see Open Threads). The 3 WARN release-gate tests against the 2026-03-19 oracle baseline (40 days of expected drift) remain pre-existing.
- **GHL sources read from `raw_ghl_v2` views** (PR #128). Per-entity views over `Raw.ghl_objects_raw` filtered by `entity_type` and aliasing `ingested_at AS _ingested_at`. The legacy `raw_ghl.ghl__<entity>_raw` tables are untouched (last-write 2026-04-19) — kept for historical reference.
- **`fct_calls_booked.{assigned_user_sk,pipeline_stage_sk,booking_time_opportunity_id}` are wired and flowing.** PR #123 wired the SQL chain; PR #135 added `booking_time_opportunity_id` (now 4,670/5,487 = 85% non-NULL). The 100% NULL `assigned_user_sk` anomaly traced through three deploys to its root cause: `/users/search` API contract (PR #138 + `00103-rav`). Post-fix: `assigned_user_sk` non-NULL = 10/5,487 (0.18%). The 99.82% NULL remainder is **legitimate LEFT JOIN orphans** — most historical bookings reference user IDs from former staff that `/users/search` doesn't return; broader coverage would need `includeDeleted=true` or a separate historical-users API path. Diagnostic attribution only — Speed-to-Lead numerator still sources first-touch identity from `raw_ghl.conversations`/`fct_outreach`, unaffected by this constraint.
- **Fanbasis money columns are NUMERIC end-to-end** (PR #120). `stg_fanbasis__{transactions,refunds}` cast amounts as NUMERIC; `fct_payments` Stripe arm flipped for UNION-ALL type-symmetry; `release_gate_revenue_detail` thresholds use `numeric '0.05'`/`numeric '0.10'` literals.
- **`bq-ingest` requires authenticated invocation.** `curl -H "Authorization: Bearer $(gcloud auth print-identity-token)" ...`
- **GHL transition snapshots remain LIVE.** `Core.fct_pipeline_stage_snapshots` compounds daily at 07:00 UTC.
- **bq-ingest consolidation Steps 1–4 shipped** (PRs #100/#102/#104 + 2026-04-28 deploy). `dee-data-ops` is the production deploy origin; `gtm-lead-warehouse` is no longer load-bearing.
- **Phase B layer-build still on main** (PRs #84/#86/#88/#90/#92): Fanbasis staging, identity-contact-payment bridge, payments/refunds rename, revenue_detail net-of-refunds.
- **Foundation intact (do not rebuild):** BigQuery + dbt + 15 staging models + `(id, _ingested_at, payload)` raw-landing discipline.
- **Headline metric (locked 2026-04-19):** unchanged.

## Last 3 decisions

- **2026-04-30 late evening (latest)** — **Closed Bug #1 (Aariz seed swap) + Bug #2 (fct_outreach cascade) end-to-end.** Four PRs merged: #141 (`marts.sql` static-seed fix — Aariz/Houssam swap at lines 2755 + 1567 + Ethan ID typo fix at 2766 + 1578; bq-ingest deployed as `00088-tsd`; runtime stack rebuilt; `pipeline-marts-hourly-7fshw` verification run flipped Aariz from 0 → 209 setter triggers + Houssam from 215 → 6); #142 (retired 3 redundant dbt marts: `sales_activity_detail`, `speed_to_lead_detail`, `fct_speed_to_lead_touch` + `stl_data_freshness` rollup + 2 release-gate tests + `_marts__models.yml`/`_marts__docs.md` cleanups); #143 (repointed `fct_outreach` to bq-ingest `Core.fct_ghl_outbound_calls` + `Core.fct_ghl_conversations` via new `2-dbt/models/warehouse/_core__sources.yml`; CE-adversarial round 1 surfaced 3 issues fixed in round 2: workflow-call automation gate via `payload.$.source != 'workflow'` excludes 173 rows, separate-stream architecture replacing cross-entity QUALIFY dedupe, `updated_at_ts DESC` tiebreaker for re-poll dedupe); #144 (retired `stg_ghl__messages` + `raw_ghl_v2.ghl__messages_raw` view). **Verification (post-nightly `25194643806`):** `Core.fct_outreach` 5,108 rows (3,950 user_sk = 77% attribution; 5,069 contact_sk = 99%); `Marts.lead_journey.assigned_sdr_name` flipped 0% → 5.3% (856/16,086); `number_of_dials` 0% → 9.4% (1,517 contacts); top SDRs: aariz menon 1,417 dials, Blagoj 908, Marco 674, Boipelo 438. BQ cleanup: dropped 6 retired tables/views + 2 orphan `mart_volume_history` rows.
- **2026-04-30 late afternoon** — **Closed the `assigned_user_sk` 100% NULL arc end-to-end.** Four PRs merged: #135 (mart-collapse PR-1, `booking_time_opportunity_id`), #136 (Lessons-Learned + state regen), #137 (post-merge state cleanup), #138 (users code fix — `/users/search` companyId param + no-page param + C1 review-driven explicit `limit=100`). Three bq-ingest deploys: `00099-jez` → `00101-xas` → `00103-rav`. On-demand dbt nightly (run `25170736965`) verified: `dim_users` populated (16 rows), `assigned_user_sk` flipped from 0/5,487 to 10/5,487. PR-2 of mart-collapse remains blocked on David answering Q3.1/Q3.2/Q3.3 in plan §8.
- **2026-04-29/30 late evening** — Y3 marts.sql retirement scoped via PR #132 (audit doc, three revision passes after multi-persona `/ce-doc-review`); PR #133 merged retiring dead HTTP analytical surface (`/ask`, `/query`, `/query/catalog`, `analyst.py`, `warehouse_queries.py`, `google-genai` dep — 698 lines). Audit revealed: (1) David's account has read 17 of the originally-DROP-classified tables in the last 27 days — "confirmed orphan" classification was wrong on framework grounds, not data; (2) `Marts.bridge_identity_contact_payment` is scope-different from dbt's `Core.bridge_identity_contact_payment` (357 vs 3,844 rows, 3 vs 5 match tiers) — not byte-equivalent, repoint needs overlap-tier reconciliation; (3) Marts pipeline is healthy post-Y1 (was misread as broken earlier in session). Y3 itself is gated on Batch 0 verification + David's disposition on ~26 tables.
- **2026-04-29/30 late evening** — Y3 marts.sql retirement scoped via PR #132; PR #133 merged retiring dead HTTP analytical surface (698 lines). Audit revealed David's account has read 17 of the originally-DROP-classified tables in 27 days — "confirmed orphan" classification was wrong; bridge scope-difference between Marts vs Core; Marts pipeline is healthy post-Y1. Y3 itself is gated on Batch 0 verification + David's disposition on ~26 tables.

## Open threads

- ~~**🟠 SDR-attribution chain (Bug #1 + Bug #2)**~~ — RESOLVED 2026-04-30 late evening via PRs #141/#142/#143/#144. Bug #1 fix verified live (Aariz 209 setter triggers in `Marts.fct_speed_to_lead`, was 0; Houssam 6, was 215 — 209-event swap reverted cleanly). Bug #2 fix verified live post-nightly `25194643806` (`Core.fct_outreach` 5,108 rows with 77% user_sk attribution; `Marts.lead_journey.{assigned_sdr_name,number_of_dials}` flipped 0% → 5.3%/9.4%). New residual Open Threads spawned by this arc: Ayaan Menon numerator-contamination decision (separate Open Thread below); deferred dbt tests for `Marts.dim_team_members` need a `sources.yml` block first; refactor target — replace the two parallel seeds (`static_ghl_user_seed` + `ghl_user_name_map`) with a single shared CTE selected from twice.
- **🟡 Ayaan Menon numerator decision.** Surfaced by adversarial review of PR #141 round 2: Ayaan (`eWA0YcbNP3rklPwRFFwM`, 814 events, role=`unknown` in `2-dbt/seeds/ghl_sdr_roster.csv`) IS in the headline numerator today as `setter_team_member_key='unknown_setter'`. `rpt_speed_to_lead_week`, `mrt_speed_to_lead_overall`, and `rpt_appt_funnel_week` do not filter by `tm.role`; `rpt_rep_scorecard_week:5737/5741` filters `setter_name NOT IN ('unknown')` — a name filter, so Ayaan passes once seeded. His 814 events DO contribute to org-wide `bookings_with_outbound_call` / `pct_within_5m` totals. Decision pending: (a) add Ayaan to both `marts.sql` seeds (raising his role to SDR per the existing CLAUDE.local.md Open Engagement Question on roster gaps), or (b) add a `tm.role IN ('SDR','Closer')` filter at the rollup layer to formally exclude unknowns. Tied to David's pending decision on Ayaan + Jake Lynch's roster role.
- **🟡 Pivot-debt: Metabase authoring tree (`3-bi/metabase/`) references retired marts.** Surfaced 2026-04-30 late evening by adversarial review of PR #142. `3-bi/metabase/authoring/dashboards/speed_to_lead.py` has 8 hardcoded `FROM dee-data-ops-prod.marts.speed_to_lead_detail` references; `dbt_metadata_sync.py:21` defaults `--select 'stl_*'` (zero matches post-retirement); `caching_config.py:38` describes the 11-stl_*-rollup contract. Metabase has been retired per `CLAUDE.local.md` since v1.6 ship 2026-04-22, but the authoring tree was never banner-archived — pre-existing pivot-debt that PR #142's mart deletions materialize. Recommended: add FROZEN banner to `3-bi/metabase/README.md` + each dashboard authoring script, or move the entire `3-bi/metabase/` tree to `docs/_archive/metabase-authoring/`. Out of scope for PR #142 (which is the dbt mart retirement, not the Metabase authoring tree). Per `.claude/rules/pivot-discipline.md` escape-valve clause.
- ~~**🟡 Live deploy of bq-ingest**~~ — RESOLVED 2026-04-30. `00084-dfl` retired the dead surface; `00099-jez` added `users`/`tags` to GHL_OBJECT_TYPES; `00101-xas` added GHL_ENDPOINT_TAGS; `00103-rav` shipped PR #138 code fix + GHL_COMPANY_ID. Live revision is `00103-rav`.
- ~~**🟡 Verify `assigned_user_sk` flips from 100% NULL after next prod nightly.**~~ — RESOLVED 2026-04-30 14:32 UTC via on-demand dbt nightly run `25170736965`. Final state: 10/5,487 non-NULL (0.18%). The 99.82% NULL remainder is legitimate orphan case (historical/deleted/automation users not in `/users/search`); for broader SDR-attribution coverage see new Open Thread on `includeDeleted=true` below.
- **🟡 GHL users `includeDeleted=true` for historical SDR attribution.** PR #138 gets 16 active users polled. The `assigned_user_sk` 99.82% NULL on `fct_calls_booked` is mostly historical bookings whose pre-booking opp owner is a former staff user ID not returned by `/users/search`. Note: post-Bug-#2 fix, `fct_outreach`-based attribution also goes through the same 16-user `dim_users` (only ~75% of payload user_ids resolve). For broader coverage, options: (a) extend `fetch_entity_page` users branch to also call `/users/search?includeDeleted=true` and merge results; (b) add a separate `historical_users` entity type. Decision deferred until a downstream consumer surfaces the need.
- **🟡 `bq-ingest` C1 review fix not yet redeployed.** Commit `8e8978c` on main adds explicit `limit=100` to /users/search call to prevent silent truncation if user count grows past GHL server default. D-DEE has 16 users so this is future-risk only. Lands automatically on next bq-ingest deploy via `gcloud run deploy --source services/bq-ingest`.
- **🟡 Decommission `GCP_SA_KEY_PROD` secret + revoke `dbt-prod@dee-data-ops-prod` keys** — secret is dormant post-WIF cutover. Recommend deleting after the 2026-04-30 08:00 UTC scheduled nightly confirms WIF stability under cron context. `gh secret delete GCP_SA_KEY_PROD` (irreversible).
- **🟡 Drop orphan lowercase `staging`/`warehouse`/`snapshots` datasets** — partial materialization from the failed PR-#128 deploy at 19:48 UTC 2026-04-29. Currently same row counts as Y1 capitalized targets (both VIEWS over the same source data), but FUTURE risk: when staging logic next changes, only `STG.*` updates while `staging.*` definitions stay frozen. Drop with `bq rm -r --dataset` once confirmed nothing reads from lowercase names.
- ~~**2026-04-30 08:00 UTC scheduled nightly = first compound verification.**~~ — SUPERSEDED 2026-04-30 14:21 UTC. On-demand workflow_dispatch run `25170736965` succeeded (post-deploy verification of users polling fix). Tomorrow's 08:00 UTC scheduled nightly is now the first true *cron-context* verification (workflow_dispatch and cron may differ in env / working directory). Less load-bearing now that the workflow has had multiple successful workflow_dispatch runs today.
- **Stripe staging `safe_cast` rule violation** (NEW from PR #120 review). `stg_stripe__charges.sql:28-30` uses `safe_cast(... as int64)` for amount columns. Per `staging.md` SAFE_CAST should only appear in WHERE clauses. Stripe is historical-only (frozen data); fix is a small follow-up PR with careful CI build.
- **dabi NUMERIC rendering verification** (NEW post-PR-#120). First dabi pipeline run after Y1 will surface whether NUMERIC columns render as expected. If not, mart-layer `format()` cast is the right place.
- **Multi-page contacts unit test** (NEW post-PR-#119/#127). bq-ingest has no test infra; reviewer recommended a stub-mocked test that returns two pages with `searchAfter` cursors and asserts page-2 body shape (and the `operator: range` filter shape, given the operator has been wrong twice).
- **🟢 Mart cleanup post-#123 — PR-1 merged (PR #135).** Multi-PR refactor scoped at `docs/plans/2026-04-30-mart-collapse-fct-sks-plan.md`. PR-1 widened `fct_calls_booked` with `booking_time_opportunity_id` (canonical mart-side join axis) + 3 singular tests — strictly additive, no consumer change yet. PR-2 will collapse `sales_activity_detail.closer_and_outcome` behind a parity gate; PR-3 retires the gate. **PR-2 blocked on David answering Q3.1 (closer_name semantics: booking-time vs current vs both), Q3.2 (lead_journey current pipeline state), Q3.3 (outcome-column drift acceptance) in plan §8.** `lead_journey.latest_opportunity` is intentionally out of scope — answers a contact-grain "current state" question the booking-grain fact does not.
- **Y3 audit landed (PR #132)** — `marts.sql` retirement scope is defined: 22 confirmed-orphan + 5 cascade-hold + 1 parity-gated + 2 superseded + 15 PORT, post-#133 redistribution noted via banner. **Blocked on Batch 0 verification:** (1) `region-us.INFORMATION_SCHEMA.JOBS_BY_PROJECT` 27-day scan against the 19 confirmed-orphan + 9 ex-warehouse_queries.py-only PORT tables; (2) David disposition on the ~17 tables with his ad-hoc reads + the 9 newly-PORT-pending tables; (3) bridge overlap-tier reconciliation (`altimate-data-parity` against Core filtered to 3 overlapping tiers). Multi-day to multi-week depending on `mart_master_lead_wide` 1:1-vs-split fork (Open Q 2). Trigger to start: dabi reaches a natural checkpoint OR a `marts.sql` failure incident. Memory anchor: David ran query for 17 tables already; raw data captured in PR #132 thread.
- **`pipeline-marts-hourly` operating notes** (corrections from PR #115 review). Timeout 3600s; `run_marts_with_dependencies` is fail-fast; marts Job at `:50` is the only writer of `mrt_speed_to_lead_daily` (post-#117).
- **Step 5 (optional)** — Cloud Build trigger watching `services/bq-ingest/**`. Reframed as deploy provenance, not stale-clone defense.
- **Step 6** — archive `heidyforero1/gtm-lead-warehouse` + delete stale local clones at `~/Documents/{fanbasis-ingest,gtm,gtm-lead-warehouse}`.
- **bq-ingest pre-existing deferred follow-ups (per audit §"Deferred follow-ups"):** Cloud Run Jobs image rebuild path, `1-raw-landing/` consolidation, secret hygiene, orphan SQL audit (5 spec-only files), services/ polyrepo precedent.
- **Snapshot architecture follow-ups:** (a) extend `Core.fct_ghl_opportunities` upstream to surface `assigned_to_user_id`; (b) once `Core.fct_pipeline_stage_snapshots` has ≥2 daily partitions, swap GHL freshness signal from `MAX(_ingested_at)` to `MAX(snapshot_date)`.
- **Calendly drain follow-ups (deferred from PR #110 review):** advisory lock; `mode` column on `calendly_backfill_state`; FAILED-run handling.
- **Vestigial v2-cutover artifacts** — `ghl-incremental-v2`, `calendly-incremental-v2`, `ghl-backfill-v2` Jobs + scripts referencing them. Delete or label.
- **Pre-existing stale PRs:** #50 + #44 (Metabase, both predate dabi pivot). Close or evaluate.
- **GHL trusted-copy decision** — single named blocker for several Tier B / refresh marts.
- **Fathom → GHL contact join key** — attendee email reliability. Affects future `fct_calls_held`.
- **Empirical tuning of corpus-engine constants** — `DIVERSITY_RELEVANCE_THRESHOLD=0.30` + `QUALITY_PARITY_FLOOR=0.6` ship provisional.
- **Strategic-Reset-paused threads:** Typeform `form_id` upstream gap, GHL `messages`/`tasks` 0-row upstream (GHL `users` resolved 2026-04-30 via PR #138 + revisions `00099-jez`/`00101-xas`/`00103-rav`; tags also resolved — 45 rows now flowing), Fathom transcript landing, Stripe staleness.
- **Week-0 client asks** — unchanged.

## Where to look

- **bq-ingest production:** Cloud Run service `bq-ingest`, revision `bq-ingest-00103-rav` (deployed 2026-04-30 14:15 UTC; PR #138 code fix for `/users/search` + env `GHL_COMPANY_ID`). URL: `https://bq-ingest-mjxxki4snq-uc.a.run.app`. Rollback options: `bq-ingest-00101-xas` (`GHL_ENDPOINT_TAGS` only), `bq-ingest-00099-jez` (`users`+`tags` in `GHL_OBJECT_TYPES`), `bq-ingest-00084-dfl` (PR #133 dead-surface retirement, prior to env fixes), `bq-ingest-00093-xiv` (PR #127 GHL contacts fix, tag `pagination-fix`).
- **WIF setup:** Pool `github-actions`, provider `github-oidc` in project `project-41542e21-470f-4589-96d` (location global). SA `sa-transform@project-41542e21-470f-4589-96d` bound via `roles/iam.workloadIdentityUser`. Workflows: `dbt-{nightly,deploy,docs}.yml` use `workload_identity_provider:` instead of `credentials_json:`.
- **Y1 schema cutover:** dbt prod targets `STG`/`Core`/`Marts` (capitalized) datasets. Snapshots → `Core` (project-level `+target_schema: Core` in dbt_project.yml). Seeds → `STG`.
- **`raw_ghl_v2` views:** Per-entity views over `Raw.ghl_objects_raw`; created out-of-band 2026-04-29. Each view filters by `entity_type` and aliases `ingested_at AS _ingested_at`. dbt sources YAML's `schema: raw_ghl_v2`.
- **bq-ingest dependency audit:** `docs/discovery/bq-ingest-dependency-audit.md`
- **bq-ingest consolidation plan:** `docs/plans/2026-04-28-bq-ingest-consolidation-plan.md`
- **Operational-health rule:** `.claude/rules/operational-health.md`
- **bq-ingest source (canonical):** `services/bq-ingest/` in this repo.
- **Redeploy command (durable):** `gcloud run deploy bq-ingest --source services/bq-ingest --region us-central1 --project project-41542e21-470f-4589-96d --memory=1024Mi`. Add `--no-traffic --tag <name>` for parity workflows.
- **Cloud Run Jobs redeploy** (separate image): `cd services/bq-ingest && ops/scripts/deploy_runtime_stack.sh` rebuilds `fanbasis-python-runner:latest` AND updates Jobs from `ops/cloud/jobs.yaml`.
- **bq-ingest service rules:** `.claude/rules/bq-ingest.md` (path-scoped to `services/bq-ingest/**`); empirical anchor names range-operator + searchAfter contract.
- **Live snapshot table:** `project-41542e21-470f-4589-96d.Core.fct_pipeline_stage_snapshots`, partitioned by `snapshot_date`, clustered by `(pipeline_id, status)`.
- **Canonical roadmap:** `docs/discovery/gold-layer-roadmap.md`
- **Phase A → B ADR:** `docs/decisions/2026-04-27-phase-a-to-b-transition.md`
- **Mart architecture commitment:** `docs/discovery/coverage-matrix.md` + `.claude/rules/mart-naming.md` Rule 2
- **Phase B artifacts (on main):** `2-dbt/models/staging/fanbasis/`, `2-dbt/models/warehouse/{facts,bridges}/`, `2-dbt/models/marts/{lead_journey,revenue_detail,sales_activity_detail,speed_to_lead_detail}.sql`
- **Local dev loop:** `2-dbt/scripts/local-ci.sh` + `2-dbt/profiles.yml` (`dev_local` / `ci_local`) + `2-dbt/README.md` "Local CI" section.
- **Data-engineer agent + LAW skills:** `~/.claude/agents/data-engineer.md`. Pairing rule: `.claude/rules/use-data-engineer-agent.md`.
- **Corpus engine v2:** `.claude/skills/ask-corpus/scripts/` + `SKILL.md`
- **Fast operating loop:** `docs/runbooks/operator-fast-loop.md`
- **Shared portable kit:** `/Users/david/Documents/agent-kit`
- **Codex parity:** `AGENTS.md` + `.agents/skills/{ask-corpus,skill-creator,worklog}/`
- **Sprint artifacts:** `docs/discovery/{source-inventory,source-shapes,staging-models,gap-analysis,insights-summary,business-area-map,coverage-matrix,gold-layer-roadmap}.md`
- **Memories (auto-loaded):** see `MEMORY.md`
- **Engagement context / locked metric:** `CLAUDE.local.md` (gitignored overlay)
- **Portable conventions:** `CLAUDE.md` + `.claude/rules/*.md`
- **Routing rule for end-of-session:** `.claude/rules/worklog.md`
- **Pivot-discipline rule:** `.claude/rules/pivot-discipline.md`
- **Full history:** `grep -n "^## " WORKLOG.md`

## _meta

- Last regen: 2026-04-30 late evening UTC (Bug #1 + Bug #2 closure: PRs #141, #142, #143, #144 merged; project-state regen via this PR; bq-ingest service deploy `00088-tsd` + runtime stack rebuild + manual marts trigger `pipeline-marts-hourly-7fshw` + dbt nightly `25194643806`).
- WORKLOG: skipped — session output fully captured by the 4 PR descriptions, the project-state regen (this), and the in-rule edits (`live-by-default.md` HISTORICAL marker; `_facts__models.yml` fct_outreach refresh). Per `.claude/rules/worklog.md` routing table: PR descriptions + rule files + this regen replace a WORKLOG entry.
- Earlier _meta entries (PR #107/#108 morning, Step 4 deploy, reconciliation sweep, PR #118-#130 evening, Y3 audit / PR #133) — narratives carried by their respective PR descriptions and rule files.
- Recurring drift pattern (caught four times now): claiming "live revision" or "X is broken/wired" without verifying via `gcloud run services describe` / row-count queries / etc. Today's specific lesson: PR #123 wiring `assigned_user_sk` in code looks correct but the prod table was 100% NULL because the upstream service env var was missing `users`. Future-Claude on any "is X populated?" question: query the actual data, do not infer from "the code is correct" alone. The corollary of the existing `__TABLES__` lesson — code correctness ≠ data correctness ≠ infrastructure-config correctness.
- Recurring audit-completeness pattern (caught three times in PR #132's review passes): each round of `/ce-doc-review` revealed structural data-flow gaps the prior round missed (R1: cascade dependencies + analyst.py runtime consumer; R2: 3 more cascade refs + path-B HOLD contradiction + bridge scope-difference; R3: David-as-ad-hoc-consumer pattern via `INFORMATION_SCHEMA.JOBS_BY_PROJECT`). Lesson: static-grep audits of analytical pipelines miss human-operator reads; INFO_SCHEMA scan should be the FIRST verification step on any "what is consumed?" question, not the last.
- New empirical anchor (2026-04-30 GHL users investigation): `_<source>__sources.yml` coverage notes (e.g. `_ghl__sources.yml:33-37`) are a load-bearing diagnostic surface — they document which raw-source row counts are zero/healthy/degraded. Future PR review for any new SK / fact column should reconcile the source-coverage notes against the column's expected populated-state. PR #123 review missed this; PR #135's verification baseline caught it.
- New empirical anchor (2026-04-30 late evening SDR-attribution investigation): a single-row data-entry typo in a hard-coded SQL seed (`marts.sql:2755` Aariz→Houssam name swap) is undetectable by current dbt tests because (a) the seed lives in `services/bq-ingest/sql/marts.sql`, NOT a dbt model — no `unique`/`relationships` test runs against it; (b) the seed produces a *valid* `team_member_key` that resolves cleanly into `dim_team_members` and joins cleanly into `fct_speed_to_lead`; the bug is semantic (wrong human), not structural (broken FK). Cross-cutting lesson: any hard-coded identity seed needs at minimum a `unique(team_member_key)` test AND a `relationships` test against `Core.dim_users(user_id)` to catch the next typo. **Prerequisite for adding the tests:** `Marts.dim_team_members` is built directly by `services/bq-ingest/sql/marts.sql:2643` (`CREATE OR REPLACE TABLE`, not a `ref()`-able dbt model), so `unique`/`relationships` tests need either (a) a `sources.yml` source block declaring `Marts.dim_team_members`, or (b) a thin dbt wrapper model that selects from it. Until then, the lesson stays as a hardening target. **Sub-lesson:** same seed pattern was cloned at `marts.sql:1567` (`ghl_user_name_map`) for `mart_master_lead_wide.closer_name` — the two seeds desync silently. Refactor target: replace both with a single shared CTE selected from twice. **Sub-lesson:** roster CSV ↔ seed casing convention is unpinned — `ghl_sdr_roster.csv` mixes lowercase (`aariz/menon`) with Title Case because `Core.dim_users` returns GHL `$.name` verbatim. A future "name cleanup" PR could re-Title-case the roster and silently break joins on `assignedToName`. Soft pin added in PR #141 (comments on both seeds citing GHL-verbatim). Future-Claude reviewing any seed-SQL diff: grep for ALL parallel seeds referencing the canonical user_id; cross-reference each row against `2-dbt/seeds/ghl_sdr_roster.csv`; verify user_ids appear in `Raw.ghl_objects_raw` payloads; preserve GHL-verbatim casing.
- New empirical anchor (2026-04-30 late evening Bug #2 4-mart cascade): an empty fact (`Core.fct_outreach` = 0 rows) silently zero'd out attribution columns across 4 downstream marts (`sales_activity_detail`, `speed_to_lead_detail`, `lead_journey`, `fct_speed_to_lead_touch`) for ~7 days post-Y1-cutover with no alarm. Root cause: `raw_ghl_v2.ghl__messages_raw` view filtered `entity_type='messages'` but `Raw.ghl_objects_raw` has no such entity (actual outbound entities are `outbound_call_logs` + `conversations`). Lesson: a `relationships` test passes when the FK is NULL (auto-excluded), so 100% NULL columns produce no test failure. Future hardening — add `dbt_expectations.expect_column_values_to_not_be_null_proportion_to_be_between` (or similar) on load-bearing attribution columns to catch silent zero'ing. Architectural lesson: the `_<source>__sources.yml` coverage-notes pattern (already present at `_ghl__sources.yml:29-37`) caught the data-side gap — the issue was that the staging model and the view kept reading from the obsolete entity_type rather than getting repointed during the cutover. Coverage notes need to flag "view filter likely-stale" alongside "0 rows".
- New empirical anchor (2026-04-30 late evening dbt concurrency): triggering a manual `dbt-nightly.yml` run via `gh workflow run` while a push-triggered `dbt-deploy.yml` run is in flight causes a `dbt build --target prod` race condition. Both runs `CREATE OR REPLACE TABLE` against the same prod schemas (STG/Core/Marts), and the deploy run's mid-flight model fails on "Not found: Table STG.<seed>" when the nightly's seed step drops+recreates the same table during the window. Caught with PR #144's auto-deploy (failed at `dim_users` model with "Not found: STG.ghl_sdr_roster") while my manually-triggered nightly succeeded. Lesson: don't trigger a manual nightly when a push-triggered deploy is queued or in flight. Either wait for the deploy to complete before manual trigger, OR scope the manual trigger to a non-overlapping selector via `--workflow-run-args`. The deploy-failed but nightly-succeeded outcome means prod is in correct state; the deploy failure is benign noise.
