# Secrets and local config
.env
.env.local
.env.*.local
.env.metabase
ops/metabase/terraform/.terraform/
ops/metabase/terraform/tfplan
ops/metabase/terraform/*.tfstate*

# Personal / engagement-specific Claude overlay
CLAUDE.local.md

# Python
.venv/
__pycache__/
*.pyc

# dbt
target/
dbt_packages/
logs/
.user.yml

# Snapshot runtime artifacts (regenerated on every snapshot run; do not commit)
ops/bq/.last_snapshot_ts
ops/bq/.last_snapshot.log

# macOS
.DS_Store

# Editor
.vscode/
.idea/
*.swp

# Claude Code agent worktrees (ephemeral, per-track)
.claude/worktrees/
.gstack/
